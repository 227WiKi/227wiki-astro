<article class="wiki-article">
  <slot />
</article>

<style>
  .wiki-article {
    min-width: 0;
    overflow-wrap: anywhere;
    line-height: 1.75;
  }

  .wiki-article :global(:is(h2, h3, h4, h5, h6)) {
    scroll-margin-top: 6rem;
  }

  .wiki-article :global(> :first-child) {
    margin-block-start: 0;
  }

  .wiki-article :global(pre) {
    max-width: 100%;
    overflow-x: auto;
    padding: 1rem;
    border-radius: var(--radius);
  }

  .wiki-article :global(table) {
    display: block;
    max-width: 100%;
    overflow-x: auto;
  }

  .wiki-article :global(a:focus-visible) {
    outline: 2px solid var(--ring);
    outline-offset: 2px;
  }

  .wiki-article :global(h2) {
    margin-block: 2rem 0.75rem;
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 1.3;
  }

  .wiki-article :global(h3) {
    margin-block: 1.5rem 0.5rem;
    font-size: 1.25rem;
    font-weight: 600;
  }

  .wiki-article :global(p),
  .wiki-article :global(ul),
  .wiki-article :global(ol),
  .wiki-article :global(blockquote) {
    margin-block: 0.75rem;
  }

  .wiki-article :global(ul) {
    list-style: disc;
    padding-inline-start: 1.5rem;
  }

  .wiki-article :global(ol) {
    list-style: decimal;
    padding-inline-start: 1.5rem;
  }

  .wiki-article :global(a) {
    color: var(--wiki-brand);
    text-decoration: underline;
    text-underline-offset: 0.2em;
  }

  .wiki-article :global(blockquote) {
    border-inline-start: 3px solid var(--border);
    color: var(--muted-foreground);
    padding-inline-start: 1rem;
  }
</style>
