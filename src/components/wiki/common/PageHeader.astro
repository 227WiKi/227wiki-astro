---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
}

const { eyebrow, title, description } = Astro.props;
---

<header class="flex max-w-3xl flex-col gap-2">
  {eyebrow && <p class="text-sm font-medium text-(--wiki-brand)">{eyebrow}</p>}
  <h1 class="font-heading text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h1>
  {description && <p class="text-pretty text-muted-foreground">{description}</p>}
</header>
